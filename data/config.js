//require postgres node module:
const pg = require ('pg');

//set database connetion credentials:
const config = {
    host: 'localhost',
    user: 'jaerentredway',
    database: 'practice_api'
};

// TODO: create a postgres pool
// const pool = pg.createPool(config);

// TODO: export the pool
//module.exports = pool;
